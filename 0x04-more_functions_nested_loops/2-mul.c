#include "main.h"

/**
* mul - check description
* @a : first number parameter
* @b : second number parameter
* Description: multiplies two numbers passed to it
* Return: the product of the numbers
*/
int mul(int a, int b)
{
	return (a * b);

}
